# Contact Form Configuration
# Salt for IP address hashing (use a secure random string in production)
CONTACT_SALT=your-secure-salt-here-change-in-production

# Contact adapter type (log, email, database, api)
CONTACT_ADAPTER=log

# Rate limiting configuration (optional overrides)
# CONTACT_RATE_LIMIT_WINDOW=3600000
# CONTACT_RATE_LIMIT_MAX=3

# Email service configuration (when using email adapter)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-password
# CONTACT_TO_EMAIL=contact@yourdomain.com
# CONTACT_FROM_EMAIL=noreply@yourdomain.com

# Database configuration (when using database adapter)
# DATABASE_URL=postgres://user:pass@localhost:5432/contacts

# API service configuration (when using API adapter)
# CONTACT_API_URL=https://api.example.com/contact
# CONTACT_API_KEY=your-api-key

# Security settings
# CSRF_SECRET=your-csrf-secret
# SESSION_SECRET=your-session-secret

# Honeypot settings
# HONEYPOT_FIELD_NAME=website
# HONEYPOT_TIME_THRESHOLD=3000